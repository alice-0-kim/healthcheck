<?php
/**
 * Implements hook_menu()
 */
function healthcheck_menu() {
  $items['healthcheck/scom'] = array(
    'title' => 'Health Check',
    'route_name' => 'healthcheck',
  );
  return $items;
}
?>
